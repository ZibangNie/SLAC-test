二、我建议的下一轮参数（平衡版）

目标：

保留 insert 已恢复的能力，同时尽量把整体 boundary F1 拉回去。

我建议下一轮不要退回旧 baseline，也不要继续更猛，而是做一个 balanced-ins 版本。

1. 建议的数据集版本名
全量 Refiner 数据集
/root/autodl-tmp/SLAC-test/SLAC/refiner/data/refiner_real_dataset_canonical_insbalance
训练集
/root/autodl-tmp/SLAC-test/SLAC/refiner/data/refiner_real_dataset_core1024_insbalance
长文压力集
/root/autodl-tmp/SLAC-test/SLAC/refiner/data/refiner_real_dataset_longstress_insbalance
2. 建议的下一轮噪声参数（平衡版）
K = 6
p_shift = 0.45
p_insert = 0.12
p_delete = 0.35

discrete_laplace_scale = 2.0

max_insert_frac = 0.12
max_delete_frac = 0.35
max_shift_frac = 0.45

burst_delete_prob = 0.25
burst_delete_min = 2
burst_delete_max = 3
相比这一轮，变化是

p_delete: 0.40 -> 0.35

p_shift: 0.50 -> 0.45

p_insert: 0.15 -> 0.12

max_delete_frac: 0.40 -> 0.35

max_shift_frac: 0.50 -> 0.45

max_insert_frac: 0.15 -> 0.12

burst_delete_prob: 0.35 -> 0.25

burst_delete_max: 4 -> 3

设计意图

保留缺失真边界监督

降低 insert 过度激进导致的 FP

降低 chunk 过细倾向

尝试把 boundary precision 和整体 F1 拉回去

3. 建议的下一轮 loss 参数（平衡版）
insert_pos_weight = 16.0
alpha_insert = 3.0
alpha_edit = 1.0
beta_cost = 0.0

其余不变：

alpha_offset = 0.5
lambda_del = 1.0
lambda_ins = 1.0
lambda_shift = 0.25
相比这一轮，变化是

alpha_insert: 4.0 -> 3.0

insert_pos_weight 保持 16.0

设计意图

保留 insert 正类加权

略微降低 insert loss 在总 loss 中的主导程度

尝试恢复 edit/boundary 的整体平衡

4. 建议的下一轮训练参数

训练主干先不改，保持可比性：

epochs = 6
batch_size = 1
lr = 8e-5
weight_decay = 1e-2
doc_layers = 1
window_size = 8
seed = 13

也就是说，下一轮只改：

数据噪声

insert 相关 loss 强度

不改：

模型结构

窗口

学习率

这样实验最干净。

三、建议的实验命名
当前这轮
real_core1024_insboost_d1w8
下一轮建议
real_core1024_insbalance_d1w8
四、我对下一轮的预期
理想结果

insert_prf 从当前 0.27~0.31 略降或持平都可以

boundary F1 从当前 0.564 回升到 0.58+

edit acc 比这一轮更高

num_chunks 不要继续明显上升

mean_atoms_per_chunk 略回升

不理想信号

如果下一轮出现：

insert_prf 又掉回接近 0
那说明回调过头了

如果出现：

boundary F1 继续低

insert_prf 也没更好
那说明不是强度问题，而可能要改更真实的噪声构造方式