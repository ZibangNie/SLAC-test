Supplementary File
March 12, 2008

Supplementary File
March 12, 2008


Theoretical comparison between MDE and MLE

Both minimum distance estimator and maximum likelihood estimator belong to the Minimum distance (MD) family. Given the vector of parameters of interest θ 0 ∈ Θ where Θ is the set of possible parameter values, MD estimator can be generalized as minimizing a criterion function:
F (θ) =ĝ(θ)D w (θ)(1)
whereĝ(θ) is a function of the data y t that will verifyĝ(θ 0 ) → 0, and D w (θ) being a weighted distance matrix. Depending on the choices ofĝ(θ), different estimators can be generated.

In particular, a minimum divergence estimator, which incorporates minimum distance and maximum likelihood, is proposed [1] as an alternative to non-parametric density estimation. Density-based minimum divergence methods include those estimate parameters through minimizing some pre-defined divergence between the assumed model density and the true model density underlying the data, e.g. maximum likelihood method and minimum chi-squared method. The criterion is given by
θ = arg min θ [ f (x|θ) 1+α dx − 1 + α nα n i=1 f (x i |θ) α ](2)
with a metaparameter α > 0. MDE corresponds to α = 1 while MLE corresponds to α → 0.

An example of the two estimation criteria, for normal density X ∼ N (µ, σ 2 ):
µ MLE = arg max µ n i=1 log φ(x i |µ, σ 2 ) (3) µ MDE = arg min µ ( 1 2σ √ π − 2 n n i=1 φ(x i |µ, σ 2 ))(4)
While the aim of MDE is to maximize the sum of the densities, MLE tries to maximize the product of the densities.

Simulated dataset is generated from the following patterns, with parameters generated from normal distributions:   
x1(i, j) = 0.1 + sin(1/3j) + ε(i, j) x2(i, j) = −0.1 + sin(1/3j − 1) + ε(i, j) x3(i, j) = 1.2sin(2/5j − 2) + ε(i, j) (5) x4(i, j) = 1.5sin(1/3j − 3.5) + ε(i, j) x5(i, j) = 0.5sin(2/5j − 2.2) + ε(i, j) x6(i, j) = 0.6sin(1/3j − 3.8) + ε(i,
Figure 1 :
1The resulting clusters by the partial regression clustering algorithm for the simulated dataset. The right plot in the third row is the whole dataset and the left plot are the outliers.

Figure 2 :
2The original partition, bottom right plot is the whole dataset

Robust and efficient estimation by minimising a density power divergence. A Basu, I R Harris, N L Hjort, M C Jones, Biometrika. 85A. Basu, I.R. Harris, N.L. Hjort, and M.C. Jones. Robust and efficient estimation by minimising a density power divergence. Biometrika, 85:549-559, 1998.